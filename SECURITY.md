# Security policy

Please report security issues privately to the maintainers rather than opening a public issue.

The highest-risk surface is the RL task sandbox. Treat imported repositories and model-generated commands as untrusted. Network access must remain disabled by default, hidden verifiers must live outside editable workspaces, and credentials must never be mounted into an episode.

Until a private reporting address is published, open a GitHub security advisory on the repository.
